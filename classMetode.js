'use strict';
/*
class Humans {
    constructor(name) {
        this.name = name;
        this.damage = this.getDamage();
    }

    getDamage() {
        if (this instanceof Snipers) {
            return 30;
        } else if (this instanceof Tanks) {
            return 100;
        }
    }
} //end of main class
class Snipers extends Humans {
    constructor(name) {
        super(name);
        // this.damage = Math.floor(Math.random() * 20);
    }
}
class Tanks extends Humans {
    constructor(name) {
        super(name);
        // this.damage = Math.floor(Math.random() * 40);
    }
}
//let danilo = new Humans('Danilo');
//let tank1 = new Tanks('Tank1');

//console.log(tank1.damage);

class Orks {
    constructor(name) {
        this.name = name;
        this.damage = this.getDamage();
    }
    getDamage() {
        if (this instanceof Sperers) {
            return 30;
        }
    }
}
class Sperers extends Orks {
    constructor(name) {
        super(name);

    }
}
let stan = new Sperers('Stan');
console.log(stan.damage);          */

console.log(allHumans);